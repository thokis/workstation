[defaults]
inventory = inventory.ini
roles_path = roles
# Pin the target interpreter: auto-discovery can land on a broken pyenv shim,
# and only the system python3 carries the python3-apt bindings the apt module needs.
interpreter_python = /usr/bin/python3
